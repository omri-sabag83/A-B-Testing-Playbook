# A/B Testing Playbook — dependencies
# Environment: conda env `ab_testing_playbook`, Python 3.13
# Install into that env with:  pip install -r requirements.txt
#
# Packages get installed and version-pinned as each module actually needs
# them, not all upfront (per our agreed approach) — installed versions below
# are pinned to what was actually tested; everything after that is still a
# draft guess for later modules.

# --- Installed for Module 1 (Foundations) ---
numpy==2.5.3
pandas==3.0.5           # NOTE: pandas 3.x — watch for compatibility issues if/when
                         # seaborn is added later (The Seaborn Portfolio project hit
                         # a seaborn 0.13.2 / pandas 3.x rendering bug on faceted plots;
                         # unconfirmed whether it recurs here, flagging pre-emptively)
ipykernel==7.3.0         # kernel registration for VS Code / Jupyter notebook execution

# --- Installed for Module 2 (B2C Product Metrics) ---
matplotlib==3.11.2      # funnel bar chart, cohort retention-curve chart

# --- Installed for Module 3 (Statistical Foundations) ---
scipy==1.18.1            # stats.norm, ttest_ind, chi2_contingency — power/sample-size
                         # math, z-test, t-test, chi-square test

# --- Module 4 (Experiment Design): no new installs ---
# hashlib is Python's standard library (bucketing); chi-square reused scipy
# from Module 3. First module needing nothing new.

# --- Module 5 (Multiple Looks): no new installs ---
# peeking simulation, Bonferroni, and Benjamini-Hochberg all built from
# scratch on numpy/scipy already installed. Second module needing nothing new.

# --- Module 6 (Analyzing & Interpreting Results): no new installs ---
# stat-vs-practical significance, segment diagnosis, and the interference
# simulation all built from scratch on numpy/scipy/matplotlib already
# installed. Third module in a row needing nothing new.

# --- Module 7 (Common Experimentation Pitfalls): no new installs ---
# Simpson's paradox and novelty/primacy simulations built from scratch on
# numpy/scipy/matplotlib already installed. Fourth module in a row needing
# nothing new.

# --- Draft: expected for later modules, not installed yet ---
# statsmodels             — Module 8: possibly for diff-in-differences;
#                           not needed so far even for sequential testing
#                           (Module 5 built Bonferroni/BH from scratch instead)
# seaborn                 — not needed so far; matplotlib has covered every chart
#                           so far, add only if a later module's chart needs it
# jupyterlab              — only if you want browser-based Jupyter instead of the
#                           VS Code notebook UI; not installed since VS Code covers it
